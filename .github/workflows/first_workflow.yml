name: first workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubunt-latest
    steps:
      - name: hello world
        run: echo "Hello World"
        shell: bash